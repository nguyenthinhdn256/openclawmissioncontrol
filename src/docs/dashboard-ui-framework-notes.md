# Dashboard UI Framework Notes

The UI framework stays presentation-focused.
Board pages own composition, while `src/lib/seed` provides mock data and `src/lib/utils/dashboard-summary` provides rollups.
