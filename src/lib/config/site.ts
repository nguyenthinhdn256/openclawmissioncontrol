export const siteConfig = {
  name: "OpenClaw Mission Control",
  description: "Operations dashboard for orchestrating missions, dispatches, QA gates, and artifacts on top of OpenClaw.",
  version: "0.1.0"
} as const;
